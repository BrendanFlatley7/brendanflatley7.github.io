# Phone number for the PDF only.
# Copy this file to .env.local and fill it in. .env.local is gitignored, so the
# number never reaches the deployed site — only your locally generated PDF.
RESUME_PHONE=
